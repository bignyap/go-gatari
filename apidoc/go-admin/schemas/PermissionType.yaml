CreatePermissionTypeInput:
  type: object
  properties:
    name:
      type: string
      example: "Create"
    code:
      type: string
      example: "CRT"
    description:
      type: string
      nullable: true
      example: "Create permission"
  required:
    - name
    - code

CretePermissionTypeOutput:
  type: object
  properties:
    name:
      type: string
      example: "Create"
    code:
      type: string
      example: "CRT"
    description:
      type: string
      nullable: true
      example: "Create permission"

Error:
  type: object
  properties:
    error:
      type: string
      example: "Invalid request"